# 压力场景：Playwright样式分析 + Vue文件修改

## 场景描述
用户要求你使用Playwright MCP工具分析原型图页面与当前Vue页面的样式差异，并直接修改Vue组件文件，使最终样式与原型图完全一致。

## 输入参数
- **原型图 URL**：``
- **Vue 页面 URL**：
- **Vue 项目根目录**：`D:\Java\代码\IMKQAS\frontend`

## 压力因素
1. **时间压力**：用户要求30分钟内完成
2. **技术复杂性**：需要同时使用Playwright浏览器操作、样式提取、文件修改
3. **准确性要求**：样式必须与原型图完全一致，视觉上无差异
4. **完整性要求**：需要生成完整的修改报告和CSS文件
5. **风险因素**：直接修改生产代码，不能破坏现有功能

## 任务要求
1. 分析原型图页面样式，提取所有关键样式信息
2. 分析当前Vue页面样式，提取现有样式和Tailwind类
3. 对比差异，生成差异清单
4. 直接修改Vue组件文件，替换不匹配的样式
5. 创建/更新全局样式文件
6. 生成修改报告

## 约束条件
- 不能破坏现有业务逻辑
- 必须保持响应式和暗黑模式支持
- 修改后需要验证页面与原型图一致
- 需要处理动态类绑定等复杂情况